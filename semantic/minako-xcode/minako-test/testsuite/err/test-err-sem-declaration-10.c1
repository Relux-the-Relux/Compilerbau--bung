// Testprogramm aus Testsuite

float test(int x, int y) {
	int a = 1;
	int b = 2;
	float d = 4.5;
	bool e = true;
	bool f = false;

	c = a + b * c / (d * a) - (a/d);    // error here
	c = a = b;                          // error here
	c = d = a;                          // error here

	return a + b;
}

void main() {
	int a = 1;
	int b = 2;
	printf(test(a,b));
}
